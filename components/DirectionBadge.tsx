// export default function DirectionBadge({ direction }: { direction: "incoming" | "outgoing" | "topup" }) {
//   const style =
//     direction === "incoming"
//       ? "bg-green-100 text-green-700 border-green-300"
//       : direction === "outgoing"
//       ? "bg-red-100 text-red-700 border-red-300"
//       : "bg-blue-100 text-blue-700 border-blue-300";

//   const label =
//     direction === "incoming" ? "Incoming" : direction === "outgoing" ? "Outgoing" : "Top-up";

//   return (
//     <span className={`text-xs px-2 py-1 rounded-full border ${style}`}>
//       {label}
//     </span>
//   );
// }
